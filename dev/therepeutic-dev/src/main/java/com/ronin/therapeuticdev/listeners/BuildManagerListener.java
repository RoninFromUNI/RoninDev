package com.ronin.therapeuticdev.listeners;

/**
 * placeholder for build manager level events.
 * currently unused — build tracking is handled by BuildListener via
 * CompilationStatusListener. keeping this in case i need project-level
 * build lifecycle hooks later (e.g. gradle task tracking).
 */
public class BuildManagerListener {
}
